# CHANGELOG

> CREDEAL 온톨로지 및 명세 레포 버전 이력.
> 형식은 [Keep a Changelog](https://keepachangelog.com/) 를 따르되, **파괴적 변경과 마이그레이션 가능 여부를 명시**합니다.

---

## [작업 지시 v1.0] — 2026-08-25

**규격은 갖춰졌습니다. 남은 것은 구현과 실증입니다.**

| 신설 | 담는 것 |
|---|---|
| **`WORK_ORDER.md`** | **D00 최상위 작업 지시서** — 스프린트 6개 · **62.5일** · 소유 · DoD · 리스크 · 차단 3건 |
| `IM_PIPELINE_COMPLETION_SPEC.md` | **D27** 파이프라인 완성 규격 — 포스처 5종·Pack 10종 대응 보완 8건 |

### D27 — 파이프라인 재실측

먼저 정정합니다 — **"파이프라인이 포스처를 모른다"는 틀린 진단이었습니다.**
현행은 포스처 5종 추출·섹션 편성·조건부 폼 4종·등급 캡을 이미 갖췄습니다.
v0.5 대비 새로 생긴 갭은 더 구체적입니다.

| 갭 | 실측 |
|---|---|
| 🔴 **확인사항 섹션이 없습니다** | 5종 전부. 불변조건 13(결손은 확인사항으로)이 갈 자리가 없습니다 |
| 🔴 **등급 카테고리에 Pack이 0** | 운영형은 3개년 실적을 다 넣어도 **1점도 안 오릅니다**. 렌트롤 25점은 존재하지 않는데 분모에 남습니다 |
| 🔴 **포스처를 LLM이 확정** | 온톨로지는 중개인 선택. "62건 전부 income"이 기본값의 결과일 수 있습니다 |
| `Provenance` 5종·이름 상이 | `derived` 없음 → 파생값 신뢰도 상한이 타입에 없습니다 |
| 조건부 섹션 4종 | `sectional_spec` 없음 — **근린상가·지식산업센터가 주력 밴드인데** |
| 입력 순서 | 개발형은 명도 책임이 렌트롤보다 먼저인데 폼은 재무→임대 순 |
| 등급 임계 75점 | v0.5는 L×P |
| 결손 되먹임 | "다음 한 단계"를 등급 엔진이 계산하지 않습니다 |

보완 P1~P8 · 착수 순서 · 안 되는 것 5건은 D27 본문.

### D00 — 실행 계획

```
S0 시간 확보 5.0 ──┐
                   ├─▶ S2 포스처 6.0 ─▶ S3 입력 12.0 ─▶ S4 마이그 12.5 ─▶ S5 9.0 ─▶ S6 10.0
S1 문서 정합 8.0 ──┘                              🔴 V5-4 되돌릴 수 없음
```

**차단 해제 3건** — 이미지 마스킹 검출 모델 · `QG` 실제 정의 · `trading` 수집 경로.
`trading` 수요가 0이면 62.5일 → **57일**.

---

## [인계 세트 v1.0] — 2026-08-25

**문서를 더 만드는 문제가 아니었습니다.** 인계용 실행 문서 D1~D13은 이미 전부
있었고, **그 13종 5,970행이 v0.5·D19 v2.2·D22·D23을 하나도 모르는 상태**였습니다.

정의서 — `IM_HANDOVER_SET.md` (D25)

### 신설

| 문서 | 담는 것 |
|---|---|
| `IM_HANDOVER_SET.md` | **D25** 인계 세트 정의 — 완전성 기준 · 결손 · 순서 · DoD |
| `IM_STANDARD_운영형.md` | 포스처 표준 — GOP 기준 · 3개년 실적 · 용도 적법성 |
| `IM_STANDARD_개발형.md` | 포스처 표준 — **L≥R1 · P≥P3** · 명도 매도인 부담 관행 |
| `IM_STANDARD_사옥형.md` | 포스처 표준 — 자가 vs 임차 · 지가 4시나리오 강제 |
| `IM_STANDARD_단기매매형.md` | 포스처 표준 — **`internal_only`** · 출구 제약 |
| `IM_PIPELINE_RUNTIME_SPEC.md` | **D26** 실행 계약 — 실패 3종 · 재시도 · 멱등 · 시간 예산 |
| `qa/doc_integrity.py` | 문서 세트 무결성 6종 |
| `03_spec_current_state/` | **프로덕션 현행 명세 3종 편입** |
| `99_superseded/` | 폐기 9종 격리 |

### 슬롯 신설 — Pack 2종

| Pack | 왜 |
|---|---|
| `holding_history` | 🔴 `trading` L축. **없어서 그 포스처는 구조적으로 D등급**이었습니다 |
| `operating_performance` | 요양시설·주차장·골프연습장은 객실이 없습니다. `hospitality_spec`에서 분리 |

### 고친 것

- 🔴 **폐기 9종을 `99_superseded/`로 격리** — 루트에서 정본과 나란히 있어
  파일 목록에서 구분되지 않았습니다. **경로 자체가 경고가 되어야 합니다.**
- 🔴 **현행 파이프라인 명세 3종이 레포 밖에 있었습니다** — 레포는 이상적 설계를
  말하고 프로덕션은 다르게 돌았습니다. `03_spec_current_state/`로 편입
- 🔴 **포스처 계약 13칸이 1/5** → **5/5**. 내용은 `IM_STANDARD_포스처확장` v3.0과
  `POSTURE_IMPL_GUIDE`에 있었으나 **계약 형태가 아니어서 기계가 못 읽었습니다**
- `MIGRATION_RUNBOOK.md` §9 신설 — v0.5 파괴적 변경 7건 · 12단계 · 되돌릴 수 없는
  지점 1곳 추가(등급 재산정)
- `README.md` 전면 재작성 — 독자별 진입 경로 · 층위 · 정본 소유표
- `IM_STANDARD_수익형.md` §0A — 계약 13칸 명시. `L26`이 v0.5 신설인 이유
- 끊긴 포인터 2건 처리 (`PPTX_시스템_개선_요구서` §0A)

### 검사

```
python3 qa/doc_integrity.py     격리 · 포인터 · 고아 · 계약 13칸 · 버전 · CHANGELOG
python3 qa/ontology_check.py    소유 · 코드군 · 포스처 · 자릿수 · 요구서 대조
```

```
끊긴 포인터        16 → 0
포스처 계약     1/5 → 5/5 (13칸)
버전 정합       8/11 → 11/11 (v0.5)
폐기 격리          0 → 9
```

### 서브에이전트 감사에서 잡힌 것 — 전량 정정

초안을 독립 감사에 걸었더니 **차단 5 · 중대 20여 건**이 나왔습니다.
가장 아픈 것 넷:

| 유형 | 무엇이 |
|---|---|
| 🔴 **검사기가 느슨했습니다** | `doc_integrity` ④가 백틱 키워드만 봐서 **칸 이름만 나열해도 통과**. 값 칸까지 보도록 고침 |
| 🔴 **한글 파일명을 못 봤습니다** | ② 정규식이 ASCII 전용 — 루트 md의 3분의 1이 한글명이라 절반을 놓쳤습니다 |
| 🔴 **폐기 문서 인용이 무검출** | 파일명만 모아 경로를 버려서 `99_superseded/` 에 있어도 "존재"로 통과. **89건이 전량 통과하고 있었습니다** |
| 🔴 **`minResolution` 이 두 값** | 정본 §6.4는 development L≥R2, 표준은 L≥R1. **명도 책임 조건부**로 정정 |

그 밖에 — `M30`·`M31` 미등록 상태로 인용, `lAxisSlots` 표 미갱신,
`trading` 차단 근거를 `G30`으로 잘못 서술(실제는 등급), D25 §0 표가
자기 문서의 지시로 이미 거짓이 됨(두 시점으로 분리), 폐기 인용 129→실측 89,
`developmentMinResolution` 산식이 4곳에 복제(정본 1곳 + 참조로 전환).

**검사기가 "정상"을 내는 것과 실제로 정상인 것은 다릅니다.**
결함 주입 시험을 통과하지 못하는 검사기는 무력화된 것입니다.

### 아직 남은 것

- D1~D13 중 **개정 필요 10종** (3종 완료) — `IM_HANDOVER_SET.md` §5
- `trading` L축 수집 경로(등기부 파싱) 미구현 → `internal_only` 유지
- `QG01~QG16` 실제 정의 미확인 — 운영 코드 열람 필요
- 🔴 이미지 마스킹 미구현 — 검출 모델 필요

---

## [온톨로지 v0.5.0] — 2026-08-25

**축은 v0.4에서 맞췄고, v0.5는 그 위에 층위 · 확장 계약 · 코드 규율을 세웁니다.**

새 문서 — `ONTOLOGY_V0.5_SPEC.md` (v0.4는 ⛔ SUPERSEDED, 재현용 보존)

### 파괴적 변경 7건

| # | 변경 | 영향 |
|---:|---|---|
| 1 | `Provenance` 5종 → **9종** | `public` 분할 — 전 슬롯 재분류 |
| 2 | 등급을 **L×P 이중 해상도**로 | 전 딜 등급 재산정 |
| 3 | `G1~G9` → **`G01~G09`** · 운영 게이트 **`QG`** 분리 | 로그 파서·대시보드 수정 |
| 4 | **포스처 계약 미충족 시 발행 차단** (`G30`) | `trading` 발행 중단 |
| 5 | `R-INC-04~06` 정의 확정 · 라벨 4건 변경 | `L02` 분기 재작성 · 과거 딜 재판정 |
| 6 | **정의가 빈 코드 금지** | C 8 · M 12 폐기, R-INC 4 정의 확정 |
| 7 | **A등급 정의가 점수에서 L×P로 이동** | `C11`·`L04` 발동 조건이 바뀝니다 |

### 고친 것

- 🔴 **폐기 문서를 소유자로 지목하던 참조 회수** — D22-8 필지 유실 사고의 구조적 원인.
  P 계산식을 `IM_PRECISION_SPEC`(폐기)에서 `CATALOG_RULES` §2.2로,
  수집 경로표를 `IM_DATA_PIPELINE`(폐기)에서 `CATALOG_SLOTS` §1.3으로 회수
- 🔴 **`R-INC-02`가 두 문서에서 다른 뜻이었음** — `CATALOG_RULES` "밸류애드형" vs
  `CATALOG_ASSET_TYPES` "임대료 정상화형". `L02`가 이 코드로 편성을 가르므로
  당산동에 정반대 섹션이 편성될 수 있었습니다. 후자를 `R-INC-04~06`으로 이관
- 🔴 **온톨로지가 자기 금지어를 쓰고 있었음** — `초안정`·`밸류애드`.
  어휘 규칙이 수익형 전용 문서에 갇혀 있어 온톨로지 작성자가 몰랐습니다.
  `CATALOG_LEXICON` §7~§8로 승격하고 적용 범위(`LexiconScope` 3종)를 정의
- **Pack 3종 정의 신설** — `residential_spec`·`sectional_spec`·`hospitality_spec` 은
  선언만 있고 슬롯 정의가 없었습니다. 근린상가·지식산업센터는 주력 밴드입니다
- **`operating`·`trading` 아키타입 신설** (`R-OPR`·`R-TRD` 각 4종, **가설**)
- **슬롯 매트릭스 축 교체** — 폐기된 `AssetClass` → `investmentPosture` 5종
- **코드군 선언·실측 정합** — 5건 불일치 → 0
- **불변조건 21 → 23**, 전 항목에 `applies_to` 부착
- 신규 등록 — `G17~G24`·`G26~G30`·`C33~C34`·`X01~X05`·`M25~M29`

### 자동 검사

`qa/ontology_check.py` ①~⑤. 6개 지표 전량 목표 도달.

```
owning_refs_to_superseded    3 → 0
code_group_mismatch          5 → 0
empty_code_definitions      12 → 0
postures_without_archetype   2 → 0
posture_conditional_gates    0 → 26
digit_style_mismatch      true → false
```

폐기 문서 회고 언급은 `[HIST]` 태그로 구분합니다 — 어절 기반 예외
("…않습니다")는 한국어에서 사실상 전면 면제가 되어 폐기했습니다.

### 마이그레이션

`ONTOLOGY_V0.5_SPEC.md` §9.1 12단계. **롤백 가능 기간은 4단계(등급 재산정) 반영 전까지.**

### 아직 남은 것

- `IM_STANDARD_운영형`·`_개발형`·`_사옥형`·`_단기매매형` 미작성 → 계약 `sections` 미검증
- `trading` L축 슬롯(보유 이력·매도 사유) 미신설 → `internal_only`
- `QG01~QG16` 은 **코드 공간 예약만** — 운영 게이트 16종의 실제 의미 미확인
- `BASE_PROFILE` 의 Pack 단일 열 ↔ `POSTURE_ADJUSTMENT` 의 Pack 개별 키 불일치

---

## [IM v1.12] — 2026-08-24

**현행 PPTX 시스템을 D19와 대조하고 프리셋 3종을 더했습니다.**

### 판정 — 아키텍처는 좋고, 규격이 세 벌입니다

| 체계 | 어디에 | 값 |
|---|---|---|
| 데이터 품질 등급 | `deck-sequencer` | A / B / C / D |
| 렌트롤 해상도 | `IM_RESOLUTION_TIERS` | R0~R3 |
| 이중 해상도 | D19 §3.3 | **L × P** |

**같은 물건이 세 개의 등급을 갖습니다.** A~D를 L×P에서 유도하도록 정정했습니다 (D19 §3.3).

### 개선 14건 — D20 `PPTX_시스템_개선_요구서.md`

| 등급 | 건수 | 대표 |
|:-:|--:|---|
| **차단** | **5** | 등급 3체계 · **슬라이드 단위 억제**(블록 단위여야) · **출처 슬롯 0개** · comps 보강 미구분 · **라벨 절삭** |
| 중대 | 6 | 상한 24장(정본 16) · 사진 조건부 · 매매가 강조 미규약 · 교차검증 부재 · IMCore가 환경변수 뒤 · A11·A12 잔존 |
| 경미 | 3 | 문형 구분 없음 · 흑백 검증 없음 · 소득 아키타입이 시퀀스만 바꿈 |

**라벨 절삭이 D17 E01 사고의 구조입니다** — `"현재 연 순수익률 (Cap R"`.

### 추가 — 프리셋 3종 (5 → 8)

| 키 | 타깃 | 전용 면 | 상한 |
|---|---|---|:-:|
| **`jsre_field_navy`** | 국내 현장 대면·인쇄 | — | 16 |
| **`evidence_first`** | 기관 실사 · 게이트 차단 이력 | **근거·검증** | 18 |
| **`land_value_first`** | 저밀·증축 여력 자산 | **토지 가치·개발 여력** | 16 |

### 추가 — 토큰 7종 (53 → 60)

`priceHi` · `priceHiBg` · `labelCol` · `labelColTx` · `srcChip` · `srcChipBg` · `needsCheck`

**기존 5프리셋에는 기본값 주입 — 되돌릴 수 있는 변경입니다.**

### 🔴 흑백 검사가 실제 문제를 잡았습니다

| 프리셋 | 양수(green) ↔ 음수(red) 휘도차 | 판정 |
|---|--:|---|
| `jsre_field_navy` | 0.017 | **흑백 구분 불가 — 기호 병기 필수** |
| `evidence_first` | **0.001** | **흑백 구분 불가 — 기호 병기 필수** |
| `land_value_first` | 0.030 | 구분됨 |

정본 §4.7 "색 의존 금지"가 왜 필요한지를 수치가 보여줍니다.

### 추가 — D19 v2.1 §6B·§6C

| 절 | 내용 |
|---|---|
| **§6B 프리셋 계약** | 엔진 ↔ 스킨 경계 · 필수 토큰 60 · **스위치 사전 14종** · 프리셋 검증 4항 |
| **§6C 텍스트 예산 정책** | **라벨·헤더·단위·출처 절삭 금지** · 문형별 길이 |

### 기능 확장 F11~F22

**1순위 5건이 서로 물려 있습니다** — F11 등급 통합 · F12 게이팅 · F13 출처 열 · F14 comps · F15 절삭 금지.

### 데모 3종 검증

| 프리셋 | 분량 | 정본 위반 | 오류 차단 |
|---|:-:|:-:|:-:|
| `jsre_field_navy` | 13장 | **0** | **0** |
| `evidence_first` | 14장 | **0** | **0** |
| `land_value_first` | 14장 | **0** | **0** |

---

## [IM v1.11] — 2026-08-24

**입력이 산출물을 결정하도록 바꿨습니다.**

### 🔴 정정 — 인근 실거래는 공공데이터만으로 못 만듭니다

**v1.9~v1.10은 comps를 S2(공공 API)로 분류했습니다. 틀렸습니다.**

국토부 상업업무용 실거래 API 필드를 확인했습니다.

| 사실 | 영향 |
|---|---|
| **지번이 마스킹됩니다** (`1***`) | 어느 건물인지 특정 불가 |
| **집합건물은 대지면적이 공란** | **토지 평당가 비교 불가** |
| **대로변/이면 구분이 없음** | 같은 동에서 평당가 2배 차이 |

**첨부 보고서의 comps 표는 중개인 보강이 들어간 값입니다.** 출처 등급을 **S2a(원시) / S2b(보강 필요)** 로 분할했습니다.

### 추가 — 출처 등급 S2 분할

| 등급 | 의미 |
|:-:|---|
| **S2a** | 공공 API 원시 — 그대로 사용 가능 |
| **S2b** | **공공 API + 중개인 보강** — 보강 없이는 사용 불가 |

### 추가 — 이중 해상도 L × P

| 축 | 무엇 | 채우는 사람 |
|:-:|---|---|
| **L** R0~R3 | 임대차 해상도 | **중개인만** |
| **P** P0~P3 | 물건자료 해상도 | 지번 → 공공 자동 + 중개인 보강 |

### 추가 — 바텀시트 표준 (D19 §7 · 부록 A)

**43종** — 필수 9 · 권장 17 · 선택 17. 각 필드에 **여는 블록**을 매핑했습니다.

**신설 5종** — `comp_identify` · `comp_frontage` · `comp_land_sqm` · `comp_floors` · `comp_condition`

### 추가 — 블록 게이팅 (D19 §8 · 부록 B)

**블록 47종.** 입력 집합 → 블록 열림/잠김이 결정론적으로 나옵니다.

```
입력 ──▶ resolve()  ──▶ (L등급, P등급, 부족 항목)
     └─▶ evaluate() ──▶ 블록별 열림/잠김 + 잠김 사유
                     └─▶ 렌더러는 판단하지 않고 따르기만
```

### 실측 — 입력이 늘면 블록이 열립니다

| 시나리오 | L | P | 열린 블록 |
|---|:-:|:-:|:-:|
| 당산 ① 원장만 | R1 | P0 | **19 / 47** |
| 당산 ② + 지번 | R1 | P2 | **31 / 47** |
| 당산 ③ + 중개인 보강 | **R2** | **P3** | **40 / 47** |

**지번 한 줄이 12블록을 엽니다.** 나머지 9블록은 중개인이 12필드를 더 넣어야 열립니다.

### 끝까지 안 열리는 블록 7종

갱신요구권 · 대항력 · NOI · 순수익률 · 실제 대출조건 · 공실 임대 여력 · 리모델링 계획
— **전부 계약·운영·계획 정보이고 공공데이터로 대체되지 않습니다.**

### 추가 — CREDEAL 기능 요구사항 F1~F10

**F9(비교사례 보강 전용 화면)가 가장 시급합니다** — 실거래 API는 이미 붙었는데 비교표를 만들 수 없습니다.

### 구현 — 게이팅이 실제로 작동합니다

| | |
|---|---|
| `core.available_inputs()` | 물건별 실제 입력 집합 |
| `core.blocks()` | 블록 → (열림, 잠김 사유) |
| `build_pptx_kr` | **가격 근거 면이 게이팅을 따릅니다** |
| 거래 조건 면 | **"자료를 넣으면 열리는 내용" 패널** (F6) |

**KR 산출물 4종 재생성 — 정본 위반 0 · 오류 차단 0 · 사실 주장 공통 0건.**

### 추가 — 부록 A~F

A 입력사전(코드 생성) · B 게이팅 매트릭스(코드 생성) · C 실측 판정(코드 생성) · D TS 스키마 · E 렌트롤 v1.3 · F 실거래 API 필드 확인

---

## [IM v1.10] — 2026-08-24

**PPTX를 국내 IM 관행으로 다시 만들었습니다. 사진을 앞으로 냈습니다.**

### 판정 — 어색했던 이유는 디자인이 아니라 지면 문법

실물 IM 3종(당산동 제이에스 10장 · 잠원동 히트리얼티 19장 · 상가빌딩 Basic 11장)을 분해했습니다.

| # | 앞 판의 문제 | 국내 관행 |
|:-:|---|---|
| **1** | **사진이 한 장도 없었다** | **표지·개요·갤러리에 외관 사진 전진배치** |
| **2** | 완결 문장으로 도배 | 슬라이드 불릿은 **개조식(명사형)** |
| **3** | 강약이 없었다 | **매매가 적색** · 라벨 열 남색 바탕 |
| **4** | 여백만 넓었다 | 매각 조건 띠 · 개선 여지 박스로 지면을 닫음 |

**정본 §7.1은 "표·캡션은 개조식 허용"이라고 이미 적고 있었습니다.** 앞 판이 그 예외를 쓰지 않았습니다.

### 추가 — D19 §6A PPTX 지면 문법

표지 좌우 분할 · 물건 개요 4분할 · 표 6규칙 · 13면 구성 · 개조식 카피 예시

### 추가 — 실물 에셋 17매

원본 IM에서 추출했습니다 (동일 물건).

| 물건 | 에셋 |
|---|---|
| 당산동 | 외관 5 · 내부 5 · 지도 · 지적도 · 위치도 |
| 양평동 | 외관 1 · 배후 1 · 지도 · 지적도 |

**양평동은 외관 1매뿐입니다** — 사진 면에 "사진 미제출"을 남겨 자료 요청 대상임을 표시했습니다.

### 산출물 — `*_PPTX_IM_KR_R{1,2}.pptx` 4종 (각 13장)

| p | 면 | 사진 |
|:-:|---|---|
| 1 | 표지 | **외관 전면 (좌 정보패널 + 우 사진)** |
| 2 | 한 장 요약 · 제안 Point | 외관 3 |
| 3 | 물건 개요 | **외관 1 + 태그 칩 + 매각 조건 띠** |
| 4·5 | 입지 · 토지 | **지도 · 지적도** |
| 6~10 | 임대 현황 · 투자 구조 · 가격 근거 · 리스크 | — |
| 11·12 | 건물 사진 | **외부 4 · 내부 4** |
| 13 | 거래 조건 | — |

**4종 전부 정본 위반 0 · 오류 차단 0 · 사실 주장 공통 0건.**

### 변경 — 검사기 3건

§4.7 본문 11pt 검사에서 **머리말·꼬리말·캡션 제외** (위치 + 접두 기호 판별) · `◇`·`LTV`·`환산보증금` 등 공통 허용 · 연면적 공부 확정 문구 인정

---

## [IM v1.9] — 2026-08-24

**두 스펙을 통합하고 공공데이터를 결합했습니다.**

### 판정 — 어느 쪽도 낫지 않았습니다

18개 항목 중 **정본이 8개, D18이 6개** 우세했고 **4개는 둘 다 없었습니다.**

| 정본이 나은 것 | D18이 나은 것 | 둘 다 없던 것 |
|---|---|---|
| 매수인 관점 · 12p 분량 · 페이지 구성 · 첫 화면 3숫자 · 어휘 · 워크플로우 · 필수 15/8 | `basis` 강제 · 단일 코어 · 결손 파생 · 문형 12종 · 빌드 강제 · 검증 26종 | **출처 등급 · 교차검증 · R1/R2판 · 디자인 시스템** |

### 추가 — D19 `CREDEAL_IM_SPEC_수익형.md`

| 신설 | 내용 |
|---|---|
| **출처 등급 6단** | S1 공부 · S2 공공API · S3 원장 · S4 파생 · S5 가정 · 확인 필요 |
| **교차검증 X1~X4** | 건폐율·용적률·층별개요·공시지가를 서로 검산 |
| **R1판 / R2판** | 원장만 / 공공데이터 결합 |
| **디자인 시스템** | 색 12토큰 · 활자 6단 · 컴포넌트 6종 |
| **소구 문장 6원칙** | 돈부터 · 땅값 기준 · 여력은 숫자로 · 거리는 m · 같은 기준 비교 · 위험은 대응과 함께 |

### 🔴 교차검증이 공공 API 보고서의 오류를 잡았습니다

`DANGSAN_PUBLIC_API_EXTRACTION_REPORT.md` §2.2의 **층별 표 합계 기재가 자기 표와 맞지 않습니다.**

```
층별 표 실제 합             1,441.15㎡
보고서 기재 합계            1,141.15㎡   ← 원장 계 행과 같은 오기

X2 용적률 역산
  507.40㎡ × 221.80% = 1,125.41㎡ (지상)
  1,441.15 − B1 317.22 = 1,123.93㎡    차이 0.13%  ✔
  1,141.15 − B1 317.22 =   823.93㎡ → 용적률 162.38% (59.4%p 차이)  ✘
```

**공부상 연면적은 1,441.15㎡입니다.** 300㎡ 차이는 "자가/미등재 구획"이 아니라 **계 행 오기**입니다.

**양평동 층별 표는 계약면적입니다** — 합 2,213.68㎡ vs 연면적 2,490.88㎡. 차이 277.20㎡가 공용·기계실입니다. 성격이 다른 면적을 비교하지 않도록 `floorTableKind`로 구분했습니다.

### R2판이 게이트를 해소합니다

| 게이트 | R1판 | R2판 |
|---|:-:|:-:|
| C19 면적 불일치 (당산) | 차단 | **해소** |
| G21 오첨부 공부 (양평) | 차단 | **해소** |
| G19 표지≠원장 (양평) | 차단 | **차단 유지** — 계약 정보 |

### 임대차 해상도는 별개입니다

| 물건 | R1판 | R2판 |
|---|:-:|:-:|
| 당산동 | R1 | **R1** — 관리비·시작일은 공공데이터로 못 채웁니다 |
| 양평동 | R1 | **R2** — 층별개요가 임대면적을 채웁니다 |

### 산출물 8종

| | R1판 | R2판 |
|---|---|---|
| 당산동 | 모바일 · PPTX 15장 | 모바일 · PPTX 15장 · **확인 필요 7→5** |
| 양평동 | 모바일 · PPTX 15장 | 모바일 · PPTX 16장 · **해상도 R2** |

**8종 전부 정본 위반 0 · 오류 차단 0 · 사실 주장 공통 0건.**

### 변경 — 검사기 5건 정정

`최고높이`(법정 용어) 제외 · 공부 확정 연면적 인정 · 소수점 문장 분할 금지 · 출처·세율 문자열 공통 허용 · 캡션 줄바꿈 폭 분리

---

## [IM v1.8] — 2026-08-24

**D18이 정본을 참조하지 않고 작성된 것을 정정했습니다.**

### 🔴 정정 — 정본 소유 위반

README §2.2는 **"IM 내용 표준 · 어휘 · 문체"** 를 `IM_STANDARD_수익형.md`에 배정하고 있습니다. **D18 v1.0은 그 영역을 침범했고, 정본을 읽지 않고 골디락스 기준을 새로 썼습니다.**

| # | 항목 | 정본 | D18 v1.0 |
|:-:|---|---|---|
| 1 | 골디락스 정의 | **12p 필수 · 16p 상한** | "분량은 규정하지 않습니다" |
| 2 | 모바일 첫 화면 숫자 | **3개** | 4칸 |
| 3 | p2 한 장 요약 | **8블록 지정** | 임의 구성 |
| 4 | 어휘 치환 | 렌트롤·NOI·Cap Rate·포스처 미사용 | **산출물에 5종 노출** |
| 5 | §9.1 PPTX 필수 15 | 15/15 | **11/15** |

**충돌·누락 21건을 실측했습니다.** (D18 §0.2)

### 변경 — D18을 구현 바인딩으로 격하

| | |
|---|---|
| 소유 | 골디락스 정의·분량·어휘·문체 → **정본에 위임** |
| D18 | **코드·좌표·검사기·기준 산출물만** |
| §2 | 정본 조항 ↔ 구현 위치 매핑 23행 |
| §3 | 정본이 규정하지 않은 4건만 D18이 결정 |

### 추가 — `qa/standard_check.py`

정본 §3.1·§4.7·§5.5·§6.1·§6.3·§6.4·§9.1·§9.2를 직접 대조합니다. **D17 `output_qa.py`보다 상위입니다.**

### 정정 — 해상도 판정

| | |
|---|---|
| `fixtures/yangpyeong.json` | `expect.resolution: "R2"` |
| **`IM_RESOLUTION_TIERS.md` §1.3** | **R1** |

**정본이 맞습니다** — R2는 면적을 요구하는데 11행에 임대면적이 없습니다. `im_core.resolution_computed`가 픽스처 대신 **규칙으로 재판정**합니다. 픽스처 수정은 별도 PR입니다.

### 산출물 재생성

| | 이전 | **정본 준수** |
|---|---|---|
| PPTX | 13·14장 | **16장** (§3.1 상한 내) |
| 첫 화면 | Hero 4칸 | **한 줄 정의 + 숫자 3 + 임차 1줄 + CTA 3** |
| p2 | 임의 | **정본 §4.2 8블록** |
| 리스크 | 3분할 (다름) | **확인된 / 미확인 / 매수인 확인 권고** |
| 추가 | — | 감정가 주의 · 1층 비중 · 환산보증금 · ㎡·평 병기 · VAT · 만료 구분 |

**정본 위반 0 · 오류 차단 0 · 사실 주장 공통 0건.**

### 검증 — 정본 수치와 일치

정본 §4.4의 **"당산동 1층 비중 실측 54.8%"** 가 코어 계산과 일치합니다. 정본 §1.3의 **"두 실매물 모두 R1"** 도 일치합니다.

### 변경 — D17 점검기 오탐 4건 정정

`보다`·`외에` 제외어 · 비교 결과가 있을 때만 basis 검사 · `▲▼▽` 기호 허용 · R등급·층 표기는 사실 값 아님

---

## [IM v1.7] — 2026-08-24

**수익형 포스처의 골디락스 기준을 D18로 정본화하고, 실매물 2건의 기준 산출물을 만들었습니다.**

### 추가 — D18 `GOLDILOCKS_IM_SPEC.md`

D17이 사후 검사라면 D18은 **작성 규격**입니다.

| 내용 | 규모 |
|---|--:|
| 골디락스 판정 7관문 | **7** |
| 섹션별 작성 방법론 | **7절** |
| 카피 문형 | **12종** |
| 실매물 예시 | **2건 × 4산출물** |
| 규격의 빈 곳 발견 | **6건** |

**골디락스의 정의** — 확인된 사실은 빠짐없이 싣고, 확인되지 않은 항목은 "확인 필요"로 그 자리에 남긴 IM. 실패는 한 방향이 아니라 **양방향**입니다 (v3 빈 칸 · v1/v4 창작).

### 추가 — 단일 코어 `goldilocks/im_core.py`

**모든 파생값을 한 번만 계산합니다.** 원인 H(동일 지표 중복 산출)를 구조적으로 제거합니다.

```python
@dataclass(frozen=True)
class Val:
    value: float | int | None
    basis: str      # 필수 — 기준 없는 값은 만들 수 없습니다
    source: str     # ledger | official | derived | assumed | absent
```

### 추가 — 기준 산출물 4종

| 산출물 | 규모 | 자동 점검 |
|---|---|:-:|
| 당산동 모바일 IM | 25.5 KB · 섹션 7 | **차단 0** |
| 양평동 모바일 IM | 27.2 KB · 섹션 7 | **차단 0** |
| 당산동 PPTX IM | **13장** · 네이티브 표 11 | **차단 0** |
| 양평동 PPTX IM | **14장** · 네이티브 표 12 | **차단 0** |

**7라운드 산출물에 없던 A16(투자 구조) 슬라이드가 들어갔습니다** — 총취득원가 분해 · LTV 3구간 · 역레버리지 판정 · ROE 이론 상한.

### 신설 — Hero 대체 순위

D8 §3.2의 Hero 2번 칸(평당가)이 **당산동에서 계산되지 않습니다** — 대지면적이 없습니다.

```
평당가(대지) → 평당가(연면적) → 총취득원가 → 확인 필요 n건
```

**당산동은 총취득원가 121.33억, 양평동은 평당가(대지) 15,933만원/평로 각각 결정됐습니다.**

### 정정 — C19 적용 조건

**모든 행에 임대면적이 있을 때만 면적 불일치를 판정합니다.** 양평동은 B1 공실 행에만 면적이 있어(422.25㎡), 전체와 비교하면 없는 불일치가 만들어집니다.

### 변경 — D17 점검기

| # | 정정 |
|:-:|---|
| 1 | **라벨↔값 탐색 경계 3종** — `\t`(셀) · `다.`·`. `(문장) |
| 2 | **역레버리지에서 무차입 ROE 양수는 정상** — `QA-LEV-01` 예외 |
| 3 | `calibrate.py` **양방향** — 오탐 7건 + 미탐 1건 |
| 4 | `cross_property.py` **사실 주장 / 정책 문장 분리** |

### 확인 필요 1건

**D8 §3.2의 당산동 평당가 7,500만원/평의 출처** — 역산하면 대지 506.8㎡인데 픽스처에 없습니다.

---

## [IM v1.6] — 2026-08-24

**산출물 7라운드 점검 결과를 D17로 정본화했습니다.**

### 추가 — D17 `OUTPUT_QA_GUIDE.md`

D9(무엇을) · D14(단위 어떻게) · D15(E2E 어떻게)에 이어, **이미 생성된 산출물을 어떻게 검사하는가**를 담당합니다.

| 내용 | 규모 |
|---|--:|
| 결함 카탈로그 (원인 A~H) | **43건** |
| 발행 전 체크리스트 | 18항 |
| 자동 점검 검사 | **26종** |
| 회귀 테스트 | 16종 |
| 점검 안티패턴 | 7종 |

### 추가 — 원인 8번째 갈래 `H`

| 갈래 | 이름 | 최초 관측 |
|:-:|---|---|
| **H** | **동일 지표 중복 산출** | v4 PDF |

v3까지는 값이 **없거나 틀렸습니다.** v4는 값이 **여러 개**입니다 — Cap Rate 3개 · 실투자금 2개 · 자기자본수익률 2개(부호 반대). 실투자금 두 값은 무차입 기준과 LTV 50% 기준으로 **각각 정확합니다.** 라벨이 기준을 구분하지 않는 것이 결함입니다.

### 추가 — 부속 스크립트 3종

| 파일 | 역할 |
|---|---|
| `qa/output_qa.py` | 산출물(PDF·HTML·MD) 자동 점검 26종 |
| `qa/calibrate.py` | **정본 골든 무차단 확인** — 오탐이 나면 팀이 검사를 끕니다 |
| `qa/cross_property.py` | 두 물건 공통 문장 탐지 (원인 A) |

**보정 결과** — 골든 G01·G02·G06 차단 0건. 당산동 v4 PDF 차단 14건.

### 변경 — D9 `TEST_PLAN.md`

§6 회귀에 D17 §5의 16종을 연결했습니다. `RG-DUP-*` · `RG-SIGN-*` · `RG-BASIS-*` · `RG-SANITY-*` · `RG-TRUNC-*` · `RG-PRINT-*` · `RG-BADGE-*` · `RG-HARD-*`

### 확인된 개선 — v4 산출물

7라운드 중 **처음으로 다수 차단 사유가 해소**됐습니다.

| 항목 | v3 | v4 |
|---|---|---|
| §6 리스크 표제 | "검토 완료 및 **확인된** 핵심 팩트" | "추가 **확인이 필요한** 점검 항목" |
| 권리관계 | "담보대출 85억 승계 적격 판정" | "소유 구조·담보권 LOI 접수 후 열람" |
| 역레버리지 | 미표시 | **"월 약 −210만 원"** |

### 미해소

| 계층 | 상태 |
|---|---|
| **D 산식** | `IMCore` 단일 코어 전환 필요 (단계 5) |
| **E 렌더** | 마크다운 렌더러 · 서버 PDF · 라벨 절삭 금지 |

---

## [IM v1.5] — 2026-08-23

**Golden 164건이 합성 데이터임이 확인되어 재구축했습니다.**

### 이전 판단 정정 (누적 7건 · 신규 1건)

| # | 이전 | 정정 |
|---:|---|---|
| **7** | **Golden 사실 오류 0건** | **판정 불가 — 합성 데이터는 대조할 사실이 없습니다** |

v1.2의 "형식 오염 93.9%, 사실 오류 0건"에서 **뒤 절반이 검증이 아니었습니다.** 존재하지 않는 물건의 수치는 틀렸다고 말할 수 없습니다.

또한 v1.1의 **"열린 사슬" 결론이 부분적으로 틀렸습니다.** RAG는 죽어 있으나 **퓨샷 경로가 살아 있어** Golden이 시스템 자기 출력인 이상 폐순환이 성립합니다. 순환하는 것이 문체면 경미하지만, **잘못된 계산 패턴(`0.85`·`400%`·`×1.2`)이면 코드에서 상수를 지워도 예시가 다시 가르칩니다.**

### 추가 — 골든 8건 (S 3 · A 5)

| 등급 | 정의 | 건수 |
|:-:|---|--:|
| **S** | 진본 IM + 실측 렌트롤 | **3** |
| **A** | 공개 매물자료 **사실에 정박**해 표준 방법론으로 저작 | **5** |
| **C** | 순수 합성 — **격리** | 164 |

**공개 매물 기사에는 렌트롤이 없어** income 2건은 실측 렌트롤을 가진 진본으로 배정했습니다. 실사용 62건이 전부 income인데 공개 자료로는 income 골든을 만들 수 없다는 비대칭이 재구축의 핵심 제약입니다.

### 용적률 400% — 조사 5건 전부 상한 미만

| 용도지역 | 상한 | 400% 적용 시 |
|---|--:|--:|
| **제1종전용주거** | **100%** | **4.00배** |
| 제1종일반주거 | 150% | 2.67배 |
| 제2종일반주거 | 250% | 1.60배 |
| 제3종일반주거 | 300% | 1.33배 |

D4 §2.1은 1건으로 "+60% 과대"를 보였으나, **5건 중 400%를 허용하는 용도지역은 0건**입니다.

### 주력 대역 정정 — comps 공백은 상단입니다

**주력은 30억~500억 상업용 부동산입니다.** v1.2~v1.4에서 "주력인 20억 미만 꼬마빌딩"이라 기술한 것은 틀렸고, 그 결과 **하단을 문제로 지목하고 상단 공백을 놓쳤습니다.**

| 구간 | 자동 comps | 주력 대비 |
|---|:-:|---|
| 20억 미만 | ✗ | **주력 밖 — 영향 없음** |
| 30~300억 | ○ | B1·B2·B3 정상 |
| **300억 초과** | **✗** | **B4 공백 · 금액폭 42.6%** |

**`PriceBand` 타입을 D3 §1.1A에 신설**했습니다 (B1 30~80 · B2 80~150 · B3 150~300 · B4 300~500억). 퓨샷은 같은 밴드를 우선하고 **인접 1단계까지만** 허용합니다.

**불변조건 16을 개정**했습니다 — "20억 미만" → "**자동 comps 불가 구간(300억 초과 · B4)**".

### 로드맵

| 항목 | 변경 |
|---|--:|
| E3 과거 정제 | 1.5 → **0.5일** (선택) |
| **E6 Golden 재구축** | **+2.0일** |
| **합계** | **100.0 → 101.0일** |

---

## [IM v1.4] — 2026-08-23

**IM 생성 시스템의 사양을 문서 16종으로 확정했습니다.** (D14~D16은 v1.5에서 추가) DB 실측(Golden 164건 · `im_generation_jobs` 62건 · 30일 status 집계)과 실매물 5건 역설계를 근거로 하며, 이전 판단 **6건을 정정**합니다.

### 💥 파괴적 변경

| # | 변경 | 근거 |
|---:|---|---|
| 1 | **`lease_spaces`·`lease_units` → `lease_ledger` 단일 원장** | 같은 개념이 두 테이블로 분열 (3/13 · 5/13 컬럼만 사용) |
| 2 | **하드코딩 상수 22개 → 폐기 6 + 레지스트리 21종** | 출처가 기록된 상수 **0개** |
| 3 | **`YieldValue { value, basis }` 강제** — `number` 축약 금지 | gross에 "순수익률" 라벨이 붙던 문제 |
| 4 | **`investmentPosture` 기본값 제거** | 실사용 62건 전부 income = 기본값의 결과 |
| 5 | **렌더 경로에서 마크다운 재파싱 제거** (`IMCore` 직결) | 파이프 분할 재파싱이 오류를 증식 |
| 6 | **`im_generation_cost_log` → `im_generation_metrics`** | 대상 테이블 부재로 3개월간 미작동 |
| 7 | **A03 8행 제한 폐지** — 렌트롤 전량 표기 | 양평동 4행·잠원동 10행 누락 |

### 🔴 이전 판단 정정 6건

| # | 이전 | 정정 | 확인 방법 |
|---:|---|---|---|
| 1 | 주택 갱신요구권 **"4년"** (v0.4.0 항목 2) | **1회 · +2년** · 최초계약일로 산출 불가 | 법령 재확인 |
| 2 | 계측 **전무** | 뷰어 계측(Intersection Observer · dwell time) **이미 있음** | 코드 확인 |
| 3 | 모바일 본문 **16px 미만** | **`text-base` 이미 17px** (CJK 상향 적용) | `globals.css` |
| 4 | 오류 증식이 **폐순환** | **열린 사슬** — RAG 인덱스 부재로 순환 없음 | 인덱스 조회 |
| 5 | **실패율 40.9%** 최우선 | **시스템 오류 0건** · 전부 입력 검증 차단 | `result.error` 분석 |
| 6 | A16·A17 신설 = **섹션 +2 = 154초** | **아키타입은 슬라이드** · 섹션 7개 불변 | 생성 경로 확인 |

> **1번은 이 CHANGELOG의 v0.4.0 항목이 스스로 틀렸던 부분입니다.** 상가 10년은 맞고, 주택을 "4년"으로 적은 것이 오류입니다. 주택은 갱신요구권을 1회 행사하면 2년이 붙는 구조라 **행사 이력을 모르면 명도 시점을 산출할 수 없습니다.** 상가 산식을 주택에 적용하면 실측 사례에서 최대 **2년 9개월** 어긋났습니다.
>
> 2·3·5번은 전부 **"코드를 보지 않고 문서로 판단"** 해서 생긴 오류입니다. 이후 모든 DoD를 실행 가능한 쿼리로 작성했습니다 (`MIGRATION_RUNBOOK.md` §0.2).

### 추가 — 문서 16종

| ID | 문서 |
|:-:|---|
| D1 | `PRD_IM고도화.md` |
| D2 | `MIGRATION_RUNBOOK.md` |
| **D3** | **`API_TYPE_CONTRACT.md`** — 6문서가 참조 |
| D4 | `ASSUMPTION_REGISTRY.md` (+ `가정값_레지스트리.xlsx`) |
| D5 | `GOLDEN_CLEANUP_GUIDE.md` (+ `Golden_페르소나검토.xlsx`) |
| D6 | `TELEMETRY_SPEC.md` |
| D7 | `PPTX_ARCHETYPE_SPEC.md` |
| D8 | `MOBILE_GAP_SPEC.md` |
| D9 | `TEST_PLAN.md` |
| D10 | `POSTURE_IMPL_GUIDE.md` |
| D11 | `FIELD_TRANSITION_GUIDE.md` |
| D12 | `README.md` · `CHANGELOG.md` 개정 |
| D13 | `GENERATION_PERF_SPEC.md` |
| **D14** | **`UNIT_TEST_GUIDE.md`** (+ `fixtures/` 5건 · `selfcheck.py` 58항) |
| **D15** | **`E2E_TEST_GUIDE.md`** |
| **D16** | **`GOLDEN_REBUILD_SPEC.md`** (+ `golden/` 8건 · `golden_source/` 5건) |

### 추가 — 아키타입

- **A16 투자 구조** — 총취득원가 + LTV 시나리오 + 역레버리지 경고 (전 포스처)
- **A17 준공 전 마케팅** — stacking plan + 규제 시한 (development 전용)

### 폐기

| 문서 8종 | 상수 6종 | 아키타입 2종 |
|---|---|---|
| `ONTOLOGY_V0.2_SPEC` · `V0.3_SPEC` | NOI 추정 계수 0.85 | A11 |
| `ONTOLOGY_IMPLEMENTATION_GAP` | Opex Ratio 6종 | A12 |
| `IM_PRECISION_SPEC` | 개발형 용적률 400% | |
| `IM_DATA_PIPELINE` | 개발형 공사비 800만원/평 | |
| `IM_AUTHORING_SPEC` | Trading 목표 매각가 ×1.2 | |
| `MOBILE_IM_SPEC` · `_credeal_v2_extract` | Trading 비교사례 ×1.15 | |

**xlsx** — `CREDEAL_렌트롤_표준양식_v1.0`·`v1.1` 폐기. **v1.2가 정본**입니다.

### 마이그레이션

**`MIGRATION_RUNBOOK.md` 15단계 · 100.0일.** 전 단계에 검증 쿼리와 롤백 절차가 있습니다.

| 되돌릴 수 없는 지점 | 대비 |
|---|---|
| **Golden 정제 (E3)** | **착수 전 백업 테이블 생성** |
| 구 테이블 DROP | **로드맵에서 제외** — 최소 2개 분기 유지 |

### 잔존 확인 사항

| # | 항목 | 영향 |
|:-:|---|---|
| **A** | **호텔 Opex 35% ↔ GOP 마진 35%** | **운영형 GOP가 약 2배일 가능성** — `gop_price` 렌더 보류 |
| B | 실제 포스처 분포 | 기본값 제거 후 30일 재관측 |
| C | **B4(300~500억) 수임 건수 비중** | comps 대책 규모 |

---

## [v0.4.0] — 2026-08-03

**자산 분류를 3축으로 분리하고, 임대차 법령을 상가·주택으로 분기했습니다.** 구현 감사(`ONTOLOGY_SSOT_AUDIT.md`)에서 드러난 모델링 충돌 — 구현의 `AssetType` 8종과 명세의 `AssetClass` 6종이 같은 이름의 다른 것을 가리키던 문제 — 를 해소합니다.

### 💥 파괴적 변경

| # | 변경 | 근거 |
|---:|---|---|
| 1 | **`AssetClass` 폐기 → 3축 분리** (`buildingUse` × `assetType` × `investmentPosture`) | 잠원동·당산동이 용도·유형은 같고 투자관점만 다른데 v0.3으로는 구분 불가 |
| 2 | **T 규칙군 분기** — `T01~06` → `T-C-01~06` + **`T-R-01~07` 신설** | 다가구·상가주택은 주택임대차보호법 적용. 갱신요구권 10년 vs 4년 |
| 3 | **R 규칙군 기준축 이동** — 자산군 → 투자관점. `R-LND`·`R-LOG` 폐기(흡수) | 대지·물류는 유형이지 관점이 아님 |
| 4 | **등급 가중치 2단 구조** — 슬롯군(assetType×posture) × 슬롯 내부 배분 | 구현은 per-slot, 명세는 per-slot-group이었음 |
| 5 | **`CapRateBasis` 구현 명칭 채택** — `broker_equity` → `noi_price_deposit` 등 | 산식 기반 명칭이 관행 기반보다 안정적 |

### 추가

- **`CATALOG_ASSET_TYPES.md`** — 3축 값의 새 정본. 법정용도 29 · 시장유형 17 · 투자관점 5 · 조합 매트릭스 85셀
- **`ONTOLOGY_IMPLEMENTATION_GAP.md`** — 명세 v0.4 대 구현 v0.2.0 갭 9건 + 해소 순서
- **투자관점 2종** — `operating`(운영형·GOP 기준) · `trading`(단기매매형)
- **Pack 슬롯군 2종** — `residential_spec`(위반건축물 포함) · `sectional_spec`(공동담보 그룹 포함)
- **CapRateBasis 3종** — `gross_price_deposit`(실무 최빈값·누락분) · `noi_total_cost` · `gop_price`
- **제약 C29~C32** — 위반건축물 · 대지권 지분합 · GOP 기준 표기 · 공동담보 중복
- **게이트 G15~G16** — 적용 법령 확정 · 위반건축물 확인
- **아키타입 `R-INC-01~04`** — 같은 `income` 안의 IM 편성 분기 (초안정/정상화/공실/리모델링)
- **출시 불변조건 2건** — 임대차 법령 명시 · enum 레지스트리 등록 강제

### 수정

- **C20 갱신요구권** — 법령별 분기 (상가 10년 / 주택 최장 4년·1회)
- **C19 구현 명세** — 감사에서 미구현 확인. `severity: 'warning'`으로 확정

### 실증 근거

역설계 5건(잠원동·당산동·양평동·수택동·에이치에비뉴호텔)에서 도출했습니다. **17개 `assetType` 중 4종만 실증되었으며, 나머지는 실물 IM 확보 전까지 Pack을 확정하지 않습니다.**

### 마이그레이션

가능. `ONTOLOGY_V0.4_SPEC.md` §7. 롤백 한계선은 `posture ≠ income` 딜 또는 주거 임대차 딜 생성 시점입니다.

### 보류

**등급 컷 조정.** 역설계 5건 중 4건이 표준 모드에서 C 이하지만, 표본 5건은 근거가 되지 못합니다. `assetType`별 20건 확보 후 재검토합니다.

### 작성 기능 (`IM_AUTHORING_SPEC.md` 신설)

3축 모델이 작성 화면을 바꾸는 지점을 별도 정본으로 분리했습니다. **작성 흐름의 첫 질문이 하나 늘어납니다** — 주소 다음에 "누구에게 파는가"(`investmentPosture`)가 오며, 이것이 이후 모든 질문·편성·서사를 결정합니다.

| 항목 | 내용 |
|---|---|
| 3축 분류 UX | `buildingUse` 자동 · `assetType` 제안 · `posture` **기본값 없는 필수 선택** |
| 관점 변경 | 입력값 절대 삭제 금지 · 영향 요약 사전 고지 |
| 임대차 | 호실별 법령 지정 · 갱신 이력 **"모름" 선택지 필수** |
| 입력 보조 | 메모 파싱(확신 0.85↑) · 아키타입 제안 + 근거 |
| 실무 관행 | 용적률 2기준 병기 · 배열 부분 입력 · 공동담보 그룹 · 위반건축물 |
| 복수 variant | 같은 딜에 관점별 IM 2벌 (슬롯 공유 · 편성 분리) |

**공수 41.5 solo-day.** 소유 경계 정리에 따라 `IM_DATA_PIPELINE.md` §4.4의 `suggestPreciseMode`는 폐기되고 작성 명세 §4.3으로 이관되었습니다.

### 품질·교육 (`IM_QUALITY_TRAINING_PLAN.md` · `IM_SELF_REVIEW_PROMPTS.md` 신설)

실전 IM 5건에서 확인된 오류를 **5개 층위(L1 산술 / L2 기준 미명시 / L3 결손 / L4 법적판정 / L5 가정의 확정화)** 로 분류하고, 중개인이 AI 챗봇으로 자가 개선하는 8단계 프롬프트를 도출했습니다.

| 발견 | 빈도 |
|---|---|
| L2 기준 미명시 (용적률·수익률) | **5/5** |
| L3 결손 (최초계약일·취득비용·운영경비·제척) | **5/5** |
| L1 산술 오기 (당산동 300㎡ · 잠원동 40,000원) | 2/5 |
| L4 법적 불가 (당산동 401호 5% 상한 초과 인상 계획) | 1건 |
| L5 가정 확정화 (수택동 1,260% 지구단위계획 전제 미고지) | 1건 |

**설계 원칙 2가지.** ① 1단계(재계산)를 건너뛰면 이후 전부 무의미 — 틀린 숫자 위에 그럴듯한 분석이 쌓입니다. ② **AI의 법적 답변은 결론이 아니라 확인 목록** — 상임법·주임법 혼동이 잦아 3차 훈련을 "AI 오답 판별"에 배정했습니다.

훈련 공수 **12.0 solo-day**. 프롬프트 커버리지 — 실증 오류 11종 전부.

### 발행 후 관리 (`PRD_발행후관리.md` · `POST_PUBLISH_SPEC.md` 신설)

**발행 이후에 도는 최초의 코드군 2개를 신설했습니다.**

| 코드군 | 소유 질문 | 개수 |
|---|---|---:|
| **F** Freshness | 발행 후 낡았는가 | 10 |
| **S** Signal | 매수자 반응이 무엇을 말하는가 | 8 |

기존 8개 군과 충돌 없음. `E`는 향후 예외 계열을 위해 예약합니다.

**설계 핵심 — 판정의 정본은 하나입니다.** L1 산술·L2 기준은 발행 시 `C01~C32`·`G10`이 이미 판정했으므로 **재검사하지 않습니다.** AI는 `Hypothesis`·`Suggestion`만 생성하며 `Verdict`를 만들 수 없도록 타입 수준에서 차단합니다. `evidence` 없는 AI 출력은 DB 제약(`evidence_not_empty`)으로 저장이 불가능합니다.

**전략적 근거** — v3.1의 moat인 성사 관측(`outcome.recorded`)은 중개인이 발행 후 돌아와야 쌓입니다. 지금은 돌아올 이유가 없습니다. 이 기능이 재방문 이유를 만듭니다.

공수 **40.5 solo-day** (1차 P0 18.5 / 2차 P1 14.0 / 3차 8.0). AI 예상 호출 180회/월은 이론 상한의 3.6%.

### 매도인·매수인 진단 확장 (`READINESS_DIAGNOSTIC_SPEC.md` 신설)

구현된 Owner Readiness(자료 준비도)가 유용하다는 현장 피드백에서 출발해, **의사·상황 진단**으로 확장하고 매수인 쪽 공백을 메웁니다.

#### 문제 — 자료 준비도 ≠ 매각 의사

Owner Readiness 10항목은 전부 문서입니다. 자료 100점이어도 시세 +30%를 고집하면 시간 낭비고, 40점이어도 6개월 내 상속 정리 건이면 최우선입니다.

#### 💥 9번째 점수를 만들지 않았습니다

CREDEAL에는 이미 점수가 **8개**입니다 (`READINESS_SYSTEM.md` §4의 7개 + 딜 준비도 7축). Owner Readiness와 Layer Score는 **같은 10카테고리 가중치를 공유** — 사실상 같은 것의 두 이름입니다.

매각 의사를 점수로 만들면 9번째가 되므로, **신호 플래그**로만 표현합니다.

```
✗   매각 의사 지수  72점 / B등급
✓   매각 의사 신호   ⚠ 3건
```

DB에도 점수 컬럼이 없습니다. 응답이 정본이고 신호는 뷰로 파생합니다 — 판정 기준이 바뀌어도 과거 응답의 의미가 사후에 바뀌지 않습니다.

#### 자가응답은 점수원이 아닙니다

**"언제까지 파실 건가요?"에 "급하지 않아요"라고 답하는 것이 협상에 유리합니다.** 과장 편향이 구조적이라, 딜 준비도 축3(매도 의사 확실성)의 입력으로 쓰지 않습니다. 축3은 중개인 관측으로 유지합니다.

자가진단의 역할은 **중개인에게 무엇을 물어볼지 알려주는 것**입니다. 중개인 화면의 산출물은 점수가 아니라 질문 문안입니다.

#### 금기 문항을 명시했습니다

| 금기 | 왜 |
|---|---|
| "얼마나 급하신가요?" | 협상 카드를 내놓으라는 것 |
| "대출이 얼마나 있나요?" | 등기부로 확인할 것 |
| "최저 얼마까지 가능하세요?" | 진단이 아니라 협상 |

**진단과 협상의 경계**가 여기입니다.

#### 가격은 판정하지 않고 위치만 보여줍니다

실거래 밴드 위에 희망가 점 하나. "비쌉니다" 문구는 넣지 않습니다 — **중개인이 말하면 싸움이고 데이터가 보여주면 납득**입니다. 표본 5건 미만이면 밴드 자체를 표시하지 않습니다.

#### 매수인 진단 신설

기존에는 메모 파싱과 인바운드 폼뿐이었습니다. 5문 자가진단을 신설했고, **Q1 "지금 계약하면 잔금까지 며칠?"** 이 자금 여력·대출 준비·의사결정 구조를 동시에 드러내는 최고 판별 문항입니다. Q5(목적)는 `investmentPosture` 값과 동일해 매칭·IM 편성에 직결됩니다.

#### 경계

세후 실수령액 계산기는 **세무 검토 전 비활성**입니다. 범위 제시·근거 공개·세무사 확인 문구까지만 허용하고, 절세 안내·단일 확정 금액은 금지합니다.

공수 **23.0 solo-day** (세후 계산기 +4.0 별도). 출시 불변조건 **13** 추가.

### 어휘 사전 · SSoT 관리 (`CATALOG_LEXICON.md` · `ONTOLOGY_GOVERNANCE_SPEC.md` 신설)

**온톨로지에 어휘 계층이 아예 없었습니다.** `alias`·`canonical`·`b2cLabel` 전부 미정의 상태였고, `b2cLabel`은 `DEAL_CARD_SPEC.md`에서 카탈로그 등록 없이 도입되어 사용 중이었습니다.

#### 실증 — 같은 개념을 세 가지로

| 법인 | 표기 |
|---|---|
| 제이에스 (잠원동) | **"실질 용적률 247%"** |
| 제이에스 (당산동) | "용적률 221.8%" |
| Genesis (양평동) | "용적률 398.8%" |

전부 지상 연면적 기준입니다. 이 상태로는 메모 파싱(`IM_AUTHORING_SPEC` §3.2)의 확신도가 나오지 않습니다.

#### 라벨 3종 체계

```
canonical   개발·명세용     "지상 연면적 기준 용적률"
proLabel    Pro IM · PPTX  "용적률 (지상 연면적 기준)"
b2cLabel    딜카드 · Basic  "용적률"
```

`b2cLabel`이 `null`이면 대고객 산출물에 노출 불가입니다. `PPTX_TEMPLATE_SPEC.md` §16.2의 `CapRateBasis` 라벨표는 어휘 카탈로그로 **이관**되었습니다.

#### 💥 3층 편집 모델

| 층 | 편집 방법 | 재현성 영향 |
|---|---|---|
| 구조 (슬롯·규칙·계산식) | **PR only** | 치명적 |
| 값 (enum) | 제안 → 승인 → PR | 큼 |
| **어휘** | **화면 편집** | **없음** |

**화면 편집을 어휘에만 여는 근거는 재현성입니다.** alias는 입력 시점에만 작용하고, 발행·재렌더는 slot을 읽으므로 사전을 고쳐도 과거 IM 재현 결과가 바뀌지 않습니다. 구조·값은 그렇지 않습니다.

#### 수집은 이미 있는 것에서 시작합니다

`IM_AUTHORING_SPEC` §3.2의 `unmatched`가 지금은 IM 코멘트로 흘려보내고 버려집니다. **로깅만 붙이면 편집기 없이 수집이 시작됩니다.** 사전이 빈 상태로 편집기만 만들면 승인할 것이 없습니다.

#### 안전장치

- 전사 활성 alias의 `normalized`에 **DB 유니크 인덱스** — 한 단어가 두 슬롯에 매핑되는 것을 애플리케이션이 아니라 DB가 막습니다
- alias **삭제 불가**, 비활성화만 — 과거 파싱 로그의 해석이 사후에 바뀌면 안 됩니다
- 조직 관리자는 **전사 alias를 만들 수 없습니다** — 한 조직의 은어가 다른 조직 파싱을 오염시킵니다
- AI는 `suggestion`만 생성 · `Approval` 타입 생성 불가

시드 42항목 · 공수 **26.5 solo-day**. 출시 불변조건 **11·12** 추가.

### 대고객 화면 정본화 (`DEAL_CARD_SPEC.md` · `MOBILE_IM_SPEC.md` 신설)

시연자료(`CREDEAL_시연자료_정적판.html`)의 5종 산출물 중 **딜카드·모바일 IM Basic·Pro** 화면을 v0.4로 재정의했습니다.

#### 노출 3단 경계 확정

```
딜카드   전부 밴드 · 권역까지        ← 관심 유발
Basic    주요값 실수치 · 동까지       ← 검토 결정
Pro      전부 실수치 · 지번 · 법적판정 ← 매수 판단
```

단조성을 검사합니다 — 뒤 단계가 앞 단계보다 적게 공개하는 항목이 있으면 안 됩니다. 9개 항목 전부 통과.

**자료등급은 Pro에서만 밝힙니다.** 딜카드에서는 감췄는데(매수자가 등급 체계를 모르고 B·C면 불신을 부름), Pro 독자는 NDA를 쓰고 검토 중이므로 근거를 알 권리가 있습니다. 같은 지표를 티어에 따라 다르게 처리하는 첫 사례입니다.

#### 🔴 기준판 법적 오류 발견

시연자료 Pro 렌트롤의 대항력 컬럼이 **5개 호실 전부 "무"** 였습니다.

**대항력은 인도 + 사업자등록(주택은 주민등록)으로 성립하며 환산보증금과 무관합니다.** 영업 중인 임차인에게 대항력이 없다고 표기하면 매수인이 명도를 쉽게 보고, 거래 판단이 왜곡됩니다.

v0.2에서 `R10` 폐기·`T` 규칙군 신설로 이미 잡은 문제인데 **화면에 반영되지 않았습니다.** 기본값 `true`, `✗` 표기에는 근거 요구, 근거 없으면 G13 차단으로 확정했습니다.

#### 그 밖의 변경

- 아키타입 코드(`R02`)·논리기호(`∧`) 제거 — 근거 문장은 유지
- Basic의 자료등급 배지 제거
- 관점별 문 구성 — `development`·`operating`은 ④⑤⑥문이 교체됨
- 렌트롤에 **적용 법령 컬럼** 추가 (T-C/T-R)
- provenance 티어 분리 — Basic 2종 / Pro 5종
- 계측 비콘 명세 — 섹션 단위 `view.section`이 S01(이탈 집중)의 유일한 근원

#### 미비

**모바일 IM Basic 골든이 없습니다.** 가장 많이 배포되는 문서인데 화면 실물이 없습니다. `development`·`operating` 골든도 미비.

### 딜카드 정본화 (`BROKER_WORKSPACE_SPEC.md` 분리 포함)

딜카드 정의가 **P0 · P1-1 · P1-2 · POST_PUBLISH 네 문서에 흩어져 정본이 없었습니다.** 화면 표현을 이 문서로 통합합니다. 원 문서의 산정 로직·API·스키마는 그대로 유효합니다.

#### 💥 파괴적 변경 — 두 점수 체계 분리

시스템에 **0~100점 A~D 등급이 두 개** 있었습니다.

| 점수 | 질문 | 변경 |
|---|---|---|
| 자료등급 | IM을 발행할 수 있는가 | A~D **유지** |
| 딜 준비도 | 이 딜이 성사되겠는가 | A~D **폐기** → 상태어 (준비완료/보완필요/위험/정체) |

나란히 놓으면 반드시 혼동합니다. 상태어는 다음 행동을 암시하므로 준비도의 목적(행동 유도)에도 더 맞습니다. **두 점수를 같은 시야에 두지 않는다**를 규칙으로 못박았습니다 — 준비도는 개요 탭, 자료등급은 자료 탭.

#### 중복 계산 제거

7축의 축1 「자료 완비도」가 Owner Readiness 10항목을 세고 자료등급이 슬롯군을 세어 **같은 것을 두 번 다르게** 셌습니다. 축1의 출처를 자료등급으로 일원화하고 Owner Readiness는 입력 보조 UI로만 남깁니다.

축4 「권리 청결도」에는 **C32(공동담보 그룹당 1회 합산)** 를 반영했습니다. 단순 합산하면 채권최고액이 실제의 두 배가 되어 청결도가 부당하게 낮게 나옵니다.

#### 추가

- **IM variant 관리** — 같은 딜에 관점별 IM 복수 보유 (슬롯 공유 · 편성/이력 분리). 기존 화면에 개념 자체가 없었습니다
- 3축 칩 · 관점 변경 영향 요약 다이얼로그 (입력값 보존 절대 규칙)
- F·S 신호 통합 표시 — 문제 신호와 기회 신호 분리, 딜당 3건 상한
- 접근성 규칙 — 색상 단독 구분 금지 · 본문 14px · 라이트 고정 · 터치 44px
- 장애 내성 — 외부 API 실패가 카드 렌더를 막지 않음

공수 **34.0 solo-day**. 최대 항목은 variant 관리(5.0)입니다.

### PPTX 템플릿 명세 v0.4 반영 (`PPTX_TEMPLATE_SPEC.md`)

| 추가 | 내용 |
|---|---|
| **§15 관점별 덱 시퀀스** | `investmentPosture`가 슬라이드 구성·순서를 결정. `R-INC-01~04` 분기 포함 |
| **아키타입 3종** | A11 호실구성 · A12 소유자·권리구성 · A13 운영지표 (총 10 → **13종**) |
| **§16 기준 표기 강제** | 용적률 2기준 병기 · CapRateBasis 7종 표기 라벨 매핑 · GOP 병렬배치 금지 · 적용 법령 표기 |
| **§17 오류 층위 조판 대응** | 합계 하드코딩 금지 · 결손 열 유지 · 인상계획에 법정 상한 병기 |
| 금지 사항 11개 · 육안 확인 3개 · 자동 스캔 1종 | 신규 규칙을 체크리스트로 강제 |

**신규 아키타입 좌표는 기존 컬럼 패턴에 정확히 안착합니다** — A11·A12는 7:5 패턴 `rx = M+7.10+0.36 = 8.08`, A13은 7.3:4.5 패턴 `rx = M+7.30+0.28 = 8.20`. 세로 검산에서 A12·A13 우측 콜아웃이 주석(y=6.20)과 0.16~0.06" 간격이 되는 문제를 발견해 각각 1.24·1.18로 축소, 0.22"를 확보했습니다.

---

## [v0.3.0] — 2026-08-03

**자산군(AssetClass) 도입.** 온톨로지가 수익형에 암묵 최적화되어 있던 구조를 해체하고, 확장을 Pack으로 분리했습니다.

### 💥 파괴적 변경

| # | 변경 | 근거 |
|---:|---|---|
| 1 | `AssetClass`를 1급 개념으로 승격 (6종) | 잠원동 두원빌딩(개발형) 처리 중 수익형 전제가 깨짐 |
| 2 | Core / Pack 경계 도입 · `pack_slots JSONB` 확장점 | 자산군별 Core 분기를 원천 차단 |
| 3 | 가치 지표 다형화 — `Cap Rate`를 수익형 Pack 지표로 강등 | 사옥·대지에서 Cap Rate는 무의미 |
| 4 | 등급 가중치 프로파일을 자산군별로 분리 | 임차인 없는 물건이 최대 75점에 갇힘 |
| 5 | 아키타입 규칙군 접두 분리 — `R01~R09` → `R-INC-01~09` | R01~R09가 수익형 전제였음 |

### 추가

- **`not_applicable` 가중치 재배분 규칙** — v0.2 누락분. 없으면 명도 조건 물건이 부당하게 낮은 등급을 받음
- **`aboveGroundArea` 슬롯** — 실무의 "실질 용적률" 산출용. 잠원동 IM의 "247%"가 이 값 기준이었음
- **Pack 슬롯군 5종** — `physical_spec` · `development_plan` · `vacate_plan` · `occupancy_plan` · `permit_risk`
- **아키타입 규칙군 4종** — `R-OWN` · `R-DEV` · `R-LND` · `R-LOG`
- **제약 C23~C28** — 자산군별 정합성
- **레이아웃 L16~L20** — 자산군별 편성
- **enum 4계열** — `AssetClass` · `RampType` · `TemperatureZone` · `PermitKind`
- **NLG 마스크 M25~M29 코드 예약** — Pack 구현 시 본문 확정

### 문서

- `README.md` 신설 — 레포 진입점 · **정본 소유표**
- `ONTOLOGY_V0.3_SPEC.md` 신설 — v0.2 대체
- `CATALOG_SLOTS.md` 신설 — 슬롯·enum 단일 정본
- `CATALOG_RULES.md` 신설 — 규칙·제약·게이트·레이아웃 단일 정본
- `ASSET_CLASS_EXTENSION_PLAN.md` 신설 — 자산군 확장 로드맵
- `ONTOLOGY_V0.2_SPEC.md` → **보존** (재현용, 삭제 금지)

### 마이그레이션

- 전 딜에 `asset_class = 'income'` 주입 (기존 물건은 전부 수익형)
- 등급 재산정 시 **점수 무변화가 검증 조건**. 단 N/A 슬롯군이 있던 딜은 재배분으로 점수가 오름 (정상)
- 아키타입 코드 개명 · R-INC-02는 유효 대지면적 기준으로 재판정
- **롤백 가능 기간 — 첫 비수익형 딜 생성 전까지**

### 미해결

- C02·C03·C05·C06·C08~C10·C12 정의를 v0.1 원본에서 가져와야 함
- R-INC-04~06·09 정의 동일
- M01~M12 마스크 본문 동일
- 서울 외 지역 환산보증금 기준표

---

## [v0.2.1] — 예정 (v0.3에 흡수)

잠원동 케이스에서 즉시 필요성이 확인된 3건. **v0.3에 포함되어 별도 배포하지 않습니다.**

- `not_applicable` 가중치 재배분
- `aboveGroundArea` 슬롯
- `vacate_plan` 슬롯군

---

## [v0.2.0] — 2026-08-03

**provenance 5-tier와 파생값 합성 규칙 도입.** 계산값에 출처 표기가 없던 문제를 해결했습니다.

### 💥 파괴적 변경

| # | 변경 | 근거 |
|---:|---|---|
| 1 | 필지·건축물 슬롯 배열화 | 다필지·다동 물건 표현 불가 |
| 2 | provenance 4-tier → **5-tier** (`seller` 신설) | 매도인 고지가 중개인 입력과 섞여 책임 소재가 흐려짐 |
| 3 | 파생값 provenance 합성 규칙 신설 | NOI·Cap Rate에 출처 표기가 없었음 |
| 4 | `R10` 폐기 → **T 규칙군 신설** | 임대차 법적 지위를 아키타입에 넣은 것이 분류 오류 |

### 추가

- **Cap Rate 기준 파라미터화** (4종) — 관점에 따라 0.86%p 차이
- **T01~T06** 임대차 법적 지위 · **P01~P03** 필지
- **제약 C13~C22** — 특히 C21(합성 정합) · C22(시나리오는 항상 `assumed`)
- **enum 9계열 추가** (14 → 23)
- **NLG 마스크 M13~M24**
- 슬롯 70 → **122**

### 정정

| 항목 | 내용 |
|---|---|
| **R10 "대항력 없음"** | 성립하기 어려운 판정이었음. 대항력은 인도+사업자등록으로 발생하며 환산보증금과 무관 |
| **전용면적 = 연면적** | 골든 샘플에서 공용부가 0인 셈이었음. 계약면적 636.0평 / 전용 500.5평(78.7%)로 정정 |
| **근저당 채권최고액 < 실 잔액** | 성립 불가. 채권최고액 96억 / 실 잔액 80억으로 분리하고, 매수자 신규 대출 104.5억과 구분 |
| **논현동 비교사례 평당가** | 1억 415만 → **1억 412만** (182억 ÷ 174.8평) |

---

## [v0.1.0] — 2026-07 (초기)

- 표준 슬롯 70 · enum 14계열
- 제약 C01~C12 · 아키타입 R01~R10
- provenance 4-tier (`public` · `expert` · `broker` · `assumed`)
- 밴딩 · 재식별 게이트 · NLG 마스크

---

## 버전 정책

| 변경 유형 | 버전 |
|---|---|
| 자산군 추가·삭제 · 슬롯 타입 변경 · provenance 의미 변경 · 규칙 판정 변경 · 가중치 프로파일 변경 | **major** |
| Pack 추가 · 슬롯 추가(선택) · enum 값 추가 · 제약/게이트/레이아웃 추가 · NLG 마스크 추가 | minor |
| 문구 · 주석 · 오탈자 | patch |

**규칙 코드는 재사용하지 않습니다.** 폐기된 코드(`R10` 등)를 다른 의미로 다시 쓰면 과거 IM의 해석이 사후에 바뀌어 감사가 불가능해집니다.

---

## 발행 이력 호환성

| 온톨로지 | 엔진 위치 | 상태 |
|---|---|---|
| v0.3.0 | `packages/ontology` | 현행 |
| v0.2.0 | `packages/ontology/legacy/v0.2` | 보존 — **삭제 금지** |
| v0.1.0 | `packages/ontology/legacy/v0.1` | 보존 — **삭제 금지** |

발행된 IM은 `PublishRecord.ontologyVersion`으로 Pin되며, 조회 시 해당 버전 엔진으로 렌더합니다.
