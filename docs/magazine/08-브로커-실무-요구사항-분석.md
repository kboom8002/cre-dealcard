# 브로커 실무 요구사항 5건 — 현행 시스템 갭 분석 및 개선 방안

> **작성일**: 2026-08-28  
> **요구사항 출처**: 서울 소형 상업용 부동산 중개인 (1차 타깃 고객) 인터뷰  
> **분석 방법**: 5건 요구사항 × 현행 코드베이스 실구현 상태 정밀 대조

---

## 요약 대시보드

| # | 브로커 요구사항 | 현행 구현 | 핵심 갭 | 갭 등급 |
|---|---------------|:---:|---------|:---:|
| 1 | 매수자 관심 뉴스 + 취미 맞춤 주간 제공 | 🟡 부분 | 매수자별 개인화 콘텐츠 큐레이션 엔진 없음 | **Critical** |
| 2 | 국토부 월간 실거래 분석 → 매수자 맞춤 인사이트 | 🟡 부분 | 월간 주기 분석 + 매수자 프로필 매칭 없음 | **High** |
| 3 | 수신 매수자의 매수 의향/능력 파악 | 🟡 부분 | 매거진 독자→매수 의향 역추론 파이프라인 미완성 | **Critical** |
| 4 | 이메일/카톡으로 간편 전송 | 🟡 부분 | 카카오만 구현, 이메일 발송 엔진 전무 | **High** |
| 5 | 원페이지 이미지로도 전송 | 🔴 미구현 | 매거진→1장 이미지 렌더링 엔진 전무 | **Critical** |

---

## 요구사항 1: 매수자 관심 뉴스 + 취미 맞춤 주간 제공

### 브로커가 원하는 것
> "내 매수자 고객 A는 성수 꼬마빌딩에 관심이 있고 골프를 좋아함. 성수 권역 뉴스 + 골프 관련 라이프스타일 정보를 섞어서 매주 보내주고 싶음."

### 현행 구현 상태

| 구성 요소 | 현황 | 파일 |
|-----------|------|------|
| 매수자 프로필 | `buyer_intent_lite`: 권역·자산유형·예산·매수목적 저장 | [morning-intelligence/route.ts L83](file:///c:/Users/User/cre-dealcard/src/app/api/broker/morning-intelligence/route.ts#L83) |
| 구독자 프로필 | `magazine_subscribers`: 기본 인적 정보만 (전화, 이메일, 채널) | [subscriber-profile.ts](file:///c:/Users/User/cre-dealcard/src/domain/magazine/subscriber-profile.ts) |
| 관심 프로필 | `InterestProfile` 인터페이스: `assetTypes`, `regions`, `budgetRange` 정의됨 | [subscriber-profile.ts L10-17](file:///c:/Users/User/cre-dealcard/src/domain/magazine/subscriber-profile.ts#L10-L17) |
| 뉴스 큐레이션 | 권역·토픽·중요도 기반 뉴스 필터링 존재 | [market-crawlers.ts](file:///c:/Users/User/cre-dealcard/src/domain/external/market-crawlers.ts) |
| 에디션 타겟팅 | `target_segments` DB 필드 존재하나 **배포/렌더링에서 미사용** | [00063 마이그레이션](file:///c:/Users/User/cre-dealcard/supabase/migrations/00063_weekly_magazine.sql) |

### 핵심 갭

1. **취미/라이프스타일 프로필 필드 전무**: `InterestProfile`에 `hobbies`, `lifestyle_tags` 같은 비부동산 관심사 필드가 없음
2. **매수자↔구독자 연결 없음**: `buyer_intent_lite`의 풍부한 프로필과 `magazine_subscribers`가 분리되어 있음 (`client_id` FK 존재하나 미활용)
3. **개인화 콘텐츠 생성 엔진 없음**: 전체 구독자에게 동일한 에디션 발송 (1:N 브로드캐스트)
4. **비부동산 콘텐츠 소스 없음**: 골프, 와인, 미술 등 HNWI 라이프스타일 콘텐츠 수집 파이프라인 부재

### 개선 방안

| 항목 | 설명 | 우선순위 |
|------|------|:---:|
| **R1-1** 매수자 관심사 프로필 확장 | `magazine_subscribers`에 `interest_tags` (JSONB) 추가: `{ regions: [...], assetTypes: [...], hobbies: ['골프', '와인'], topics: ['세무', '밸류업'] }` | P0 |
| **R1-2** 매수자↔구독자 CRM 연동 | `broker_clients.linked_buyer_intent_ids` → `magazine_subscribers.client_id` 자동 동기화. 브로커가 고객 등록 시 매수 의향과 구독 프로필 원스톱 설정 | P0 |
| **R1-3** 매수자별 맞춤 에디션 생성 | `weekly-generator.ts`에 수신자 프로필 주입 → LLM이 해당 매수자 관심 권역 뉴스 우선 배치 + 관심 토픽 강조 | P1 |
| **R1-4** 라이프스타일 콘텐츠 수집기 | 네이버 뉴스 검색에 취미 키워드(골프장 오픈, 와인 페어 등) 추가 → `external_news`에 `topic: 'lifestyle'` 태깅 | P2 |
| **R1-5** 세그먼트별 차별 배포 | `distributeMagazine()` → `target_segments` + 구독자 `interest_tags` 매칭 → 매수자별 맞춤 콘텐츠 블록 선택 배포 | P1 |

---

## 요구사항 2: 국토부 월간 실거래 분석 → 매수자 맞춤 정보

### 브로커가 원하는 것
> "국토부에서 매월 나오는 상업용 실거래 자료를 분석해서 '당신이 관심 있는 성수 권역에서 이번 달 이런 거래가 있었고, 평당 토지가가 이 정도였다'는 식으로 매수자마다 다르게 보내주고 싶음."

### 현행 구현 상태

| 구성 요소 | 현황 | 파일 |
|-----------|------|------|
| 실거래 수집 | `fetchCommercialTransactions()`: 국토부 API (`RTMSDataSvcSh`) → `external_transactions` | [gov-premium-apis.ts L19-72](file:///c:/Users/User/cre-dealcard/src/domain/external/gov-premium-apis.ts#L19-L72) |
| 수집 주기 | **매일** Cron (08:00 KST) — 실시간 증분 수집 | [vercel.json](file:///c:/Users/User/cre-dealcard/vercel.json) |
| 분석 활용 | 모닝 인텔리전스 위젯에 최근 실거래 목록 표시 (권역 필터) | [MorningIntelligence.tsx](file:///c:/Users/User/cre-dealcard/src/components/dashboard/MorningIntelligence.tsx) |
| 매거진 활용 | `weekly-generator.ts`에서 최근 거래 데이터를 LLM 컨텍스트로 주입 | [weekly-generator.ts L440-609](file:///c:/Users/User/cre-dealcard/src/domain/magazine/weekly-generator.ts#L440-L609) |

### 핵심 갭

1. **월간 집계 분석 로직 없음**: 일별 수집은 하지만 월간 통계(거래량 전월비, 평당가 추이, 권역별 비교)를 산출하는 집계 엔진이 없음
2. **평당 토지가 산출 미구현**: `external_transactions`에 `building_area`는 있으나 대지면적(토지면적)이 없어 평당 토지가 계산 불가
3. **매수자별 맞춤 실거래 필터링 없음**: 전체 거래 목록을 동일하게 보여줄 뿐, 매수자의 관심 권역·예산대·자산유형으로 필터링하여 개인화된 인사이트를 제공하지 않음
4. **월간 에디션 자동 생성기 없음**: `monthly-generator.ts` 미구현 (07번 문서 B6-1과 연결)

### 개선 방안

| 항목 | 설명 | 우선순위 |
|------|------|:---:|
| **R2-1** 월간 실거래 집계 엔진 | `external_transactions`에서 월 단위 집계: 권역별 거래건수, 평균 거래가, 전월 대비 변동률, 최고/최저 거래 | P0 |
| **R2-2** 토지면적 데이터 보강 | 국토부 실거래 API 응답에서 대지면적 필드 추출 또는 건축물대장 API 연계 → `external_transactions.land_area` 추가 → 평당 토지가 산출 | P1 |
| **R2-3** 매수자 맞춤 실거래 인사이트 LLM | 매수자 프로필(권역, 예산, 자산유형) + 해당 권역 월간 거래 데이터 → LLM이 "고객님 관심 권역의 이번 달 핵심 거래" 맞춤 브리핑 생성 | P1 |
| **R2-4** 실거래 비교 분석 카드 | 특정 거래를 매수자 예산·기존 자산과 비교하는 시각적 카드 ("고객님 예산 대비 이 거래의 위치") | P2 |

---

## 요구사항 3: 수신 매수자의 매수 의향/능력 파악

### 브로커가 원하는 것
> "매거진 보낸 후에 누가 진짜 관심 있는지, 매수 의향과 능력이 있는지 파악하고 싶음. 열심히 읽는 사람은 진짜 사려는 사람일 가능성이 높으니까."

### 현행 구현 상태

| 구성 요소 | 현황 | 파일 |
|-----------|------|------|
| 독자 행동 추적 | `use-magazine-analytics.ts`: 페이지뷰, 스크롤(25/50/75/100%), 체류 시간, 클릭 추적 | [use-magazine-analytics.ts](file:///c:/Users/User/cre-dealcard/src/hooks/use-magazine-analytics.ts) |
| 크로스채널 스코어링 | `cross-channel-score.ts`: Vibe+Magazine+IM 3채널 점수 합산 (14일 윈도우, ≥80 핫리드) | [cross-channel-score.ts](file:///c:/Users/User/cre-dealcard/src/domain/analytics/cross-channel-score.ts) |
| 핫리드 알림 | `hot-lead-alert.ts`: ≥80점 시 카카오 알림톡으로 브로커에게 자동 알림 | [hot-lead-alert.ts](file:///c:/Users/User/cre-dealcard/src/domain/notification/hot-lead-alert.ts) |
| 구독자 참여 점수 | `computeEngagementScore()`: 열람 기사 수·자산유형·권역·최근성 기반 0~100 | [subscriber-profile.ts L39-52](file:///c:/Users/User/cre-dealcard/src/domain/magazine/subscriber-profile.ts#L39-L52) |
| 자동 매수 의향 추론 | `generateAutoIntents()`: 열람 패턴 → `AutoIntent` 초안 자동 생성 | [subscriber-profile.ts L57-73](file:///c:/Users/User/cre-dealcard/src/domain/magazine/subscriber-profile.ts#L57-L73) |
| 매물 큐레이션 | `/api/broker/clients/[id]/curation`: 매수 의향 기반 매칭 매물 추천 | [curation/route.ts](file:///c:/Users/User/cre-dealcard/src/app/api/broker/clients/[id]/curation/route.ts) |

### 핵심 갭

1. **의향/능력 종합 판단 대시보드 없음**: 핫리드 알림은 존재하나, 브로커가 "이 사람은 의향 O + 능력 O", "이 사람은 의향 O + 능력 X" 등을 한눈에 판별할 수 있는 종합 뷰가 없음
2. **매수 능력 신호 미수집**: 체류시간·클릭으로 '관심도'는 파악 가능하나, '매수 능력(예산, 법인 유무, 대출 여력)'은 추론 불가
3. **`generateAutoIntents()` → `buyer_intent_lite` 파이프라인 미연결**: 자동 생성된 매수 의향 초안이 실제 브로커 CRM(buyer_intent_lite)에 자동 저장되지 않음
4. **구독자별 상세 행동 리포트 없음**: 브로커가 특정 구독자의 열람 이력(어떤 섹션, 어떤 매물, 체류시간)을 개별 조회할 수 없음

### 개선 방안

| 항목 | 설명 | 우선순위 |
|------|------|:---:|
| **R3-1** 매수 의향/능력 매트릭스 대시보드 | 구독자 목록에 2×2 매트릭스 시각화: X축=참여 점수(의향), Y축=프로필 완성도(능력 추정) → 4사분면: 핫리드/육성대상/재접근/무관심 | P0 |
| **R3-2** 매수 능력 프로필 수집 폼 | 매거진 내 인터랙티브 폼 또는 계산기 사용 시 자연스럽게 수집: 법인 유무, 설립 연차, 예산 범위, 보유 자산 여부 → `interest_profile` JSONB 저장 | P0 |
| **R3-3** AutoIntent → buyer_intent_lite 자동 동기화 | `generateAutoIntents()` 결과를 브로커 확인 후 1-클릭으로 `buyer_intent_lite`에 등록하는 UI + API | P1 |
| **R3-4** 구독자별 행동 상세 리포트 | 특정 구독자 클릭 → 최근 30일 열람 이력(에디션, 섹션, 체류시간, 클릭 매물), 크로스채널 터치포인트 타임라인 | P1 |
| **R3-5** 매수 온도 자동 라벨링 | 행동 점수(참여도) + 프로필 점수(능력) → `매수 온도` 5단계 자동 라벨 (🔥적극검토 / 📈관심 / ⏸️관망 / ❄️냉각 / ⚪미확인) → 구독자 목록에 뱃지 표시 | P1 |

---

## 요구사항 4: 이메일/카톡으로 간편 전송

### 브로커가 원하는 것
> "카톡 안 쓰는 고객도 있고, 법인 고객은 이메일을 선호함. 둘 다 지원해야 함."

### 현행 구현 상태

| 채널 | 현황 | 파일 |
|------|------|------|
| **카카오 알림톡** | ✅ 완전 구현 — Solapi REST API, `TPL_MAGAZINE_NEW_ISSUE` 템플릿, 5건 배치 발송, SMS 폴백 | [distribute-magazine.ts](file:///c:/Users/User/cre-dealcard/src/domain/magazine/distribute-magazine.ts), [notification-service.ts](file:///c:/Users/User/cre-dealcard/src/lib/notification/notification-service.ts) |
| **이메일** | 🔴 미구현 — `subscriber_email` 필드는 DB에 존재하나 이메일 발송 서비스 없음 | [00065 마이그레이션](file:///c:/Users/User/cre-dealcard/supabase/migrations/00065_magazine_subscribers.sql) |
| **채널 선택** | 🟡 DB에 `channel` 필드(`kakao`/`email`/`both`) 있으나, 배포 로직은 카카오만 처리 | [distribute-magazine.ts L22](file:///c:/Users/User/cre-dealcard/src/domain/magazine/distribute-magazine.ts#L22) |

### 핵심 갭

1. **이메일 발송 엔진 전무**: Resend, SES, Nodemailer 등 이메일 전송 인프라가 없음
2. **이메일 HTML 템플릿 없음**: 매거진 콘텐츠를 이메일에 최적화된 HTML로 변환하는 렌더러가 없음
3. **`channel: 'email'` 구독자 무시**: `distributeMagazine()`이 `.in('channel', ['kakao', 'both'])`로 필터링하여 이메일 전용 구독자가 배포 대상에서 제외됨
4. **멀티채널 배포 오케스트레이터 없음**: 카카오/이메일을 통합 관리하는 디스패처가 없음 (현재 `rail/dispatcher.ts`는 골격만)

### 개선 방안

| 항목 | 설명 | 우선순위 |
|------|------|:---:|
| **R4-1** 이메일 발송 서비스 통합 | Resend 또는 AWS SES 기반 이메일 발송 모듈 구축 (`src/lib/notification/email-service.ts`) | P0 |
| **R4-2** 매거진 이메일 HTML 템플릿 | React Email 또는 MJML 기반 반응형 이메일 템플릿. 커버(시장 온도 뱃지) + AI 브리핑 요약 + 추천 매물 티저 + CTA(전체 읽기) | P0 |
| **R4-3** 멀티채널 배포 오케스트레이터 | `distributeMagazine()` 리팩토링: `channel === 'kakao'` → 알림톡, `channel === 'email'` → 이메일, `channel === 'both'` → 양쪽 모두 | P0 |
| **R4-4** 이메일 오픈/클릭 추적 | 이메일 내 추적 픽셀 + UTM 파라미터 → `magazine_analytics_events`에 `source: 'email'` 기록 → 크로스채널 스코어링에 통합 | P1 |
| **R4-5** 에디터 내 채널 프리뷰 | 매거진 에디터 발행 탭에서 "카카오 미리보기" / "이메일 미리보기" 토글 | P2 |

---

## 요구사항 5: 원페이지 이미지로도 전송

### 브로커가 원하는 것
> "카톡으로 링크 보내면 안 열어보는 사람도 있음. 핵심 내용을 이미지 1장으로 만들어서 카톡이나 문자로 바로 보내고 싶음. 단톡방에 올릴 수도 있고."

### 현행 구현 상태

| 구성 요소 | 현황 | 파일 |
|-----------|------|------|
| OG 이미지 | `@vercel/og`로 1200×630 OG 프리뷰 이미지 생성 (카카오 공유 시 미리보기) | [og/magazine/route.tsx](file:///c:/Users/User/cre-dealcard/src/app/api/og/magazine/route.tsx) |
| html2canvas | 지도 캡처, PDF 내보내기에서 사용 | [map-capture/page.tsx](file:///c:/Users/User/cre-dealcard/src/app/(broker)/broker/map-capture/page.tsx), [pdf-export-button.tsx](file:///c:/Users/User/cre-dealcard/src/components/broker/pdf-export-button.tsx) |
| PPTX 슬라이드 | IM용 PPTX 생성기는 존재하나 매거진용은 없음 | [pptx-renderer.ts](file:///c:/Users/User/cre-dealcard/src/domain/building/mobile-im/pptx/pptx-renderer.ts) |

### 핵심 갭

1. **매거진 원페이지 이미지 렌더러 전무**: 매거진 에디션의 핵심 내용(시장 온도, 주간 브리핑 3줄, 추천 매물 2건, 브로커 연락처)을 1장의 공유 가능한 이미지로 렌더링하는 기능이 없음
2. **이미지 직접 전송 파이프라인 없음**: 생성된 이미지를 카카오/문자로 첨부 전송하는 경로가 없음
3. **에디터에서 이미지 미리보기/내보내기 불가**: 매거진 에디터의 발행 탭에서 원페이지 이미지 프리뷰나 다운로드 기능 없음

### 개선 방안

| 항목 | 설명 | 우선순위 |
|------|------|:---:|
| **R5-1** 원페이지 매거진 이미지 렌더러 | `@vercel/og` (Satori) 또는 서버사이드 `html2canvas`/Puppeteer 기반. 1080×1920 (모바일 최적) 세로형 이미지 생성. 구성: ① 브로커 로고+이름 ② 시장 온도 뱃지 ③ AI 브리핑 핵심 3줄 ④ 추천 매물 2건 (티저) ⑤ QR코드 (전체 매거진 링크) ⑥ 브로커 연락처 | P0 |
| **R5-2** API 엔드포인트 | `GET /api/magazine/[brokerId]/[date]/image` → 원페이지 PNG/JPEG 반환 (캐시 + CDN) | P0 |
| **R5-3** 에디터 이미지 프리뷰 & 다운로드 | 매거진 에디터 발행 탭에 "📸 원페이지 이미지 미리보기" 버튼 + "다운로드" + "카카오 전송" | P0 |
| **R5-4** 카카오 이미지 메시지 전송 | Solapi MMS 또는 카카오 이미지 메시지 API로 원페이지 이미지를 직접 전송하는 배포 옵션 추가 | P1 |
| **R5-5** 커스터마이즈 템플릿 | 브로커가 테마 색상, 로고, 배치를 선택할 수 있는 2~3종 이미지 템플릿 | P2 |

---

## 5건 요구사항 × 기존 07번 문서 연결 매핑

| 브로커 요구사항 | 관련 07번 개선항목 | 신규 항목 |
|---------------|-------------------|----------|
| **#1** 매수자 맞춤 뉴스+취미 | C10-1~4 (구독자 세분화·개인화) | R1-1~5 (취미 프로필, 라이프스타일 수집기) |
| **#2** 국토부 월간 실거래 분석 | B6-1~3 (월간 생성기) | R2-1~4 (월간 집계 엔진, 토지면적 보강) |
| **#3** 매수 의향/능력 파악 | B7-3~4 (리드 퍼널) | R3-1~5 (의향/능력 매트릭스, 온도 라벨) |
| **#4** 이메일/카톡 전송 | — (기존 문서에 없음) | R4-1~5 (이메일 엔진, 멀티채널 오케스트레이터) |
| **#5** 원페이지 이미지 | — (기존 문서에 없음) | R5-1~5 (이미지 렌더러, 카카오 MMS) |

---

## 통합 구현 로드맵 (5건 요구사항 기준)

### Sprint 1 (즉시, ~2주)

> **목표**: 원페이지 이미지 + 이메일 채널 = 가장 빠른 체감 개선

| 항목 | 작업 | 규모 |
|------|------|:---:|
| **R5-1** | 원페이지 매거진 이미지 렌더러 (Satori 기반) | L |
| **R5-2** | 이미지 API 엔드포인트 | S |
| **R5-3** | 에디터 이미지 프리뷰 & 다운로드 | M |
| **R4-1** | 이메일 발송 서비스 (Resend 통합) | M |
| **R4-2** | 매거진 이메일 HTML 템플릿 | M |
| **R4-3** | 멀티채널 배포 오케스트레이터 리팩토링 | M |

### Sprint 2 (~2주)

> **목표**: 매수자 프로파일링 + 의향/능력 판별 기초

| 항목 | 작업 | 규모 |
|------|------|:---:|
| **R1-1** | 매수자 관심사 프로필 확장 (interest_tags) | S |
| **R1-2** | 매수자↔구독자 CRM 연동 | M |
| **R3-1** | 매수 의향/능력 매트릭스 대시보드 | L |
| **R3-2** | 매수 능력 프로필 수집 폼 | M |
| **R3-5** | 매수 온도 자동 라벨링 (5단계) | M |

### Sprint 3 (~2주)

> **목표**: 맞춤 콘텐츠 생성 + 월간 실거래 분석

| 항목 | 작업 | 규모 |
|------|------|:---:|
| **R2-1** | 월간 실거래 집계 엔진 | M |
| **R2-3** | 매수자 맞춤 실거래 인사이트 LLM | L |
| **R1-3** | 매수자별 맞춤 에디션 생성 | L |
| **R1-5** | 세그먼트별 차별 배포 | M |
| **R3-3** | AutoIntent → buyer_intent_lite 동기화 | M |
| **R3-4** | 구독자별 행동 상세 리포트 | M |

### Sprint 4+ (점진적)

| 항목 | 작업 | 규모 |
|------|------|:---:|
| **R2-2** | 토지면적 데이터 보강 (건축물대장 연계) | L |
| **R2-4** | 실거래 비교 분석 카드 | M |
| **R1-4** | 라이프스타일 콘텐츠 수집기 | M |
| **R4-4** | 이메일 오픈/클릭 추적 | M |
| **R4-5** | 채널별 프리뷰 토글 | S |
| **R5-4** | 카카오 이미지 메시지 전송 | M |
| **R5-5** | 커스터마이즈 이미지 템플릿 | M |

---

## 핵심 정리: 현행 시스템에서 가장 빠르게 체감될 3가지 개선

```
1️⃣ 원페이지 이미지 (R5-1~3)
   현재: 링크만 전송 가능 → 개선: 에디터에서 1-클릭으로 이미지 생성·다운로드·전송
   브로커 임팩트: ★★★★★ (카톡 단톡방, 문자 즉시 활용)

2️⃣ 이메일 채널 추가 (R4-1~3)
   현재: 카카오만 → 개선: 이메일+카카오 듀얼 채널
   브로커 임팩트: ★★★★☆ (법인 고객 커버리지 확대)

3️⃣ 매수 의향/능력 매트릭스 (R3-1, R3-5)
   현재: 핫리드 알림만 → 개선: 4사분면 매트릭스 + 매수 온도 라벨
   브로커 임팩트: ★★★★★ (영업 우선순위 즉시 판별)
```
